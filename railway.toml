[build]
buildCommand = "pip install -r requirements.txt && python preprocess.py"

[deploy]
startCommand = "gunicorn -w 1 -k gthread --threads 8 --timeout 300 --bind 0.0.0.0:$PORT server:app"
restartPolicyType = "on_failure"
